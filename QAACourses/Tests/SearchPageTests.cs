using System;

namespace QAACourses.Tests;

public class SearchPageTests
{
    /*
    var items = searchPage.GetSuggestionItems();
    Assert.IsTrue(items.Count > 0, "Підказки не знайдено");
    Assert.IsTrue(items.Any(i => i.Contains("smart", StringComparison.OrdinalIgnoreCase)), "Підказки не містять слова 'smart'");

    var products = searchPage.GetProductTitles();
    var collections = searchPage.GetCollectionTitles();
 
    Assert.IsTrue(products.Count >= 3, "Не всі продукти з’явилися у списку");
    Assert.IsTrue(collections.Contains("Smart Robots"), "Колекція 'Smart Robots' не відображається");

    Assert.IsTrue(searchPage.IsViewAllButtonDisplayed(), "Кнопка 'View all results' не відображається");
*/
}
